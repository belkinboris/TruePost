# Деплой 2026-07-16: обход блокировки РФ + фикс BUG-003

## Что изменено
1. static/app.part1.js … app.part6.js (НОВЫЕ) — app.js разрезан на 6 частей
   по 20-24 КБ по границам top-level statements (DPI РФ-провайдеров сбрасывает
   передачу крупных файлов через Cloudflare; порог между 23 КБ и 166 КБ).
   Склейка частей байт-в-байт равна app.js. Источник — КОРНЕВОЙ app.js
   (свежая версия с фиксом BUG-003: неблокирующий /config + скелет-экран).
2. static/index.html — 6 последовательных <script> вместо одного,
   кэш-бастер v=20260716a (styles.css тоже).
3. static/app.js — синхронизирован с корневым app.js (раньше расходились:
   в static лежала версия БЕЗ фикса BUG-003 — причина "деплой не сработал").
4. static/landing.html — canonical/og:url → autopost.projectsozdatel.ru.
5. app.js:2550 — текст алерта: autopost26 → autopost.projectsozdatel.ru.

## НЕ менялось
Логика, тарифы, лендинг-текст, generator.py, очередь — все locked variables
эксперимента нетронуты. Только загрузка фронта и домены.

## После деплоя проверить
1. Без VPN: https://autopost.projectsozdatel.ru — экран входа должен
   появиться за секунды (все 6 частей по ~24 КБ проходят DPI).
2. DevTools → Network: app.part1..6.js все со статусом 200.
3. Railway-логи: нет warning "product_event: отклонено событие".
4. Холодный старт: вместо вечной "Загрузки…" — скелет "Загружаем ваши каналы…".
